# Logs y Rastreos de errores
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencias (El código de terceros)
# NUNCA se sube, se instala con 'npm install'
node_modules
.pnpm-store

# Construcción de Producción (El resultado de 'npm run build')
# No se sube porque se genera automáticamente
dist
dist-ssr
*.local

# Configuración del Editor y Sistema Operativo (Irrelevante para otros)
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs
*.njsproj
*.sln
*.sw?

# IMPORTANTE: Variables de Entorno y Secretos (PELIGROSO)
# Aquí es donde van las API Keys. Si subes esto, te pueden hackear.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production

# Archivos de Testing (si se generan reportes visuales)
coverage/

# Archivos temporales de Vite
.vite/